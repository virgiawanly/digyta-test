const apiConfig = {
  apiUrl: process.env.NEXT_PUBLIC_API_URL,
  apiTokenIdentifier: 'tasks@accessToken',
  userProfileIdentifier: 'tasks@userProfile',
  defaultPageSize: 10,
};

export default apiConfig;
